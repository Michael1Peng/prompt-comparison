1. 对，需要过滤那种有可能包含提示词的文件，就是文本文件，多媒体啥的不需要考虑。
2. 完全可以接受误判，核心是先把这个链路跑起来。
3. 你来思考一下最简单的输出是什么？这个流程最后所有的发现的输出到 JSON 文件里面。

---

一个文件里面可能有多个提示词，然后每个提示词在文件的位置也不一样，所以这个列表应该是以提示词为维度了，先标注这个提示词的内容。然后标注它在哪个文件，在文件的起始和结束位置。