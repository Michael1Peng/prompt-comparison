1. 输出到固定的文件夹 ./analysis/
2. 
    - 文件路径
    - 提示词内容的摘要/片段
    - 提示词内容的摘要/片段所在的文件起始和结束位置
    - AI判断它是提示词文件的置信度
    - 提示词的类型分类（比如system prompt, user prompt等）？
3. 
    - 扫描的总文件数
    - 发现的提示词文件数量

---

1. 按照最简单的方式实现，提供一个命令就可以了。
2. 
- 显示进度条或百分比
    - 实时显示当前正在处理的文件
3. 直接输出一个失败的日志，然后跳过就好了，不要阻塞流程。
4. 只考虑最简单的实现方式，重复执行之后，每一次都从头再来。

---

1. nodejs
2. 这里的单元测试主要是为了验证流程能够跑通，给每一个新增的函数或者流程添加 两个测试用例就可以了，不需要多。
3. 这里的基础依赖按照实际情况来安装就行，不做限制。
4. 需要分模块划分

---

当前阶段不需要提取提取每个提示词的结构化信息，能把每一个提示词完整的输出出来就可以了。一个文件里面可能有多个提示词，需要把这种情况给考虑进来。
`prompt-list.json` 这个文件里面，输出提示词的时候，应该把它所在的文件 和对应的文件的起始和结束行数也要标注出来。